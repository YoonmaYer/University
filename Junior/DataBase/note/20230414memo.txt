SQL 문 종류

DML:Data Manipulation Language, 데이터 조작어, Insert, Update, Delete, Merge, Select

특징: 기본적으로 테이블 데이터만 조작하기 때문에 쿼리에 TABLE 키워드가 필요없다.
뷰에 대한 DML은 결과적으로 테이블의 데이터를 조작한다.

DDL:Data Definition Language, 데이터 정의어, Create, Alter, Drop etc..

특징: 데이터를 저장, 관리하기 위한 DB 객체를 정의
객체의 종류가 다양하므로 DML과 달리 쿼리에 객체의 카테고리를 지정해줘야하며 카테고리별로 문법이 상이할 수 있다.

DCL:Data Control Language, 데이터 제어어, GRANT (권한) TO (사용자), REVOKE (권한) FROM (사용자)

특징: 데이터, DB 객체, 시스템에 접근하는 것을 제한 또는 제어, DB 보안과 관련

서브쿼리: 쿼리 안에 쿼리가 있는 것

ANY, ALL은 서브쿼리와 함께 사용하는 다중 행 연산자이며, ANY는 조건을 만족하는 값이 하나라도 있으면 결과를 보여주고 ALL은 모든 값이 조건을 만족해야 결과를 보여준다

테이블복사: create table 새로운테이블명 (select 컬럼명 from 복사당하는테이블명);
내용만 복사되고 키값같은건 복사 x



